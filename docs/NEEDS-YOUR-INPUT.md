# Needs your input (cannot fully scrape / replicate)

Reply to each item so we can **simulate** rather than block the rebuild.

## 1. Designers / installers skills examination (`aes.et.nz`)

**Discovery:** `https://www.et.nz/secure/` **redirects to** `https://aes.et.nz/` (separate training portal). Public WP pages only market the course; the exam/auth DB is not scrapeable.

**Please tell us:**
1. Should the React rebuild use a **local/simulated exam** (questions + scoring in-repo or simple backend) as the replacement for Firebase / aes.et.nz?
2. Rough flow: login required? certificate PDF? pass mark?
3. Any sample questions / topic list we must cover?
4. Who can sit it (designers only, installers, both)?
5. May we keep a **non-indexed** snapshot of the aes.et.nz shell under `public/mirror/_external/` for UI reference only?

## 2. Members / secure area

Discovery found links to `/secure/` and `/members-home-page/`.

**Please tell us:** what should exist behind the login on the new site (drawings library, pricing, exam, both)?

## 3. Forms

Mirrored UI only for enquiry / price-request / contact (WPForms).

**Please tell us:** destination for submissions (email, Formspree, custom API, Google Sheet)?

## 4. Service areas / GBP

For local SEO structure we need the **locations and services** that match (or will match) Google Business Profile.

**Please list:** primary NAP, service regions, and every service that should get its own page.

## 5. Dead links on live site

These 404 on www.et.nz today: `/brochure-3/`, `/faq/`, `/residential/`.

**Please tell us:** recreate, redirect, or drop?
